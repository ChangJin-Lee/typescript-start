// const AISLE = 0
// const MIDDLE = 1
// const WINDOW = 2

// if (seat === 0 ){

// }

// const 붙은 것 과 아닌 것의 차이.
const enum SeatChoice {
    AISLE = 2,
    MIDDLE = "MIDDLE",
    WINDOW = "WINDOW",
    FOURTH = 0,
    LAST
}

const hcSeat = SeatChoice.AISLE

